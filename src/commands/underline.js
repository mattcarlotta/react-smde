import React from "react";
import SvgIcon from "~icons";

const underline = {
	name: "underline",
	tooltip: "Add an underline",
	buttonProps: { "aria-label": "Add an underline" },
	icon: <SvgIcon icon="underline" />,
};

export default underline;
